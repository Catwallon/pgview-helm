apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgview.fullname" . }}
  labels:
    {{- include "pgview.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pgview.selectorLabels" . | nindent 4 }}
